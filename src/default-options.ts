import type { Options } from "$types";

const options: Options = {
  suffix: "daw",
  ignoredKeys: [],
  invertCustomColours: true,
  invertDefaultColours: false,
  nonInvertBehaviour: "same-dark",
};

export default options;
